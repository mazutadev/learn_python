#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
Примеры лучших практик комментирования кода
"""

# Инициализация переменной для демонстрации
x = 0

# Плохой комментарий (очевидный код)
x = x + 1  # Увеличиваем x на 1

# Хороший комментарий (объясняет почему)
x = x + 1  # Компенсируем смещение индексации


def process_data(data):
    """
    Обрабатывает входные данные.

    Args:
        data (list): Список чисел для обработки

    Returns:
        dict: Статистика по обработанным данным
    """
    # Инициализация счетчиков
    total = 0
    count = 0

    # Подсчет суммы и количества
    for num in data:
        total += num
        count += 1

    # Вычисление среднего значения
    average = total / count if count > 0 else 0

    # Формирование результата
    return {"total": total, "count": count, "average": average}


# Разделение логических блоков
# ----------------------------
# Обработка пользовательского ввода
# ----------------------------


def validate_input(value):
    """
    Проверяет корректность входных данных.

    Args:
        value (str): Входная строка

    Returns:
        bool: True если данные корректны, иначе False
    """
    # Проверка на пустую строку
    if not value:
        return False

    # Проверка на допустимые символы
    if not value.isalnum():
        return False

    return True
